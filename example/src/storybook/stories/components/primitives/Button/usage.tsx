import React from 'react';
import { Button } from 'native-base';

export default function () {
  return (
    <Button borderColor="orange.2" variant={'solid'} size="xs">
      Button Variants
    </Button>
  );
}
